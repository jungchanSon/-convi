stages:
  - review

variables:
  REGISTRY: "$CI_REGISTRY_IMAGE"

mr_review:
  stage: review
  image: rnrmf/mr-review-bot:latest
  variables:
    CI_PROJECT_ID: $CI_PROJECT_ID
    GIT_LAB_KEY: TEST_KEY
  only:
    - merge_requests
  script:
    - python review_buddy.py